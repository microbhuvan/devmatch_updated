services:
  backend:
    build:
      context: ./backend
    container_name: devmatch-backend
    env_file:
      - ./backend/.env
    ports:
      - "3000:3000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: devmatch-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
